# logmile 산출물 기준 정리표

- 프로젝트명: `logmile`
- 문서 버전: `v1.9`
- 작성 기준일: `2026.05.26`
- 변경 기준: 실제 프로젝트 구현물 검증 결과, 시뮬레이터 3열 레이아웃 개편 및 오토 데모, 다중 이력 로컬 저장 복구 반영, AI 에이전트 브랜치 전략 v1.6 반영, md 우선 작업 원칙
- 버전 운영 원칙: `초안 및 정리 단계는 v1.x 유지`, `초안 완료 후 v2.x로 전환`
- 관리 원칙:
  - `md`: Git 기준 원본 문서
  - `docx`: 제출 및 보관용 문서
  - 산출물 수정은 `md`를 우선 작업하고, `docx`는 사용자가 요청한 경우에만 생성 또는 최신화한다.
  - 버전 표기가 없는 산출물은 다음 수정 시점부터 새 버전을 작성하여 관리한다.

## 1. 기준 원칙

| 구분 | 기준 |
|---|---|
| 버전 정책 | 산출물 관리 문서는 `v1.x` 기준으로 유지하되, 실제 구현 반영 핵심 문서는 해당 문서의 최신 구현 기준 버전을 우선한다. |
| 문서 원본 | 내용 수정의 기준 원본은 `md`로 관리한다. |
| 제출 문서 | 사용자가 요청하거나 제출/보관이 필요한 경우에만 `docx`를 생성 또는 최신화한다. |
| 반영 원칙 | 기준 문서(`md`)를 먼저 수정하고, `docx`는 요청 시 해당 버전 기준으로 별도 반영한다. |
| DOCX 보관 | 생성된 `docx`는 우선 `logmile_deliverable/docx/`에 보관하고 추후 정리한다. |
| 문서 상태 표기 | `초안` 표기가 있는 문서는 아직 확정 전 문서로 본다. |
| 구현 검증 기준 | 코드/SQL 기준은 `logmile_be`, `logmile_fe`, `logmile_ai`, `logmile_sim`, `logmile_infra`의 실제 파일을 우선한다. |

## 2. 최우선 기준 문서

아래 4개 문서를 현재 프로젝트의 핵심 기준 문서로 관리한다.

| 문서명 | Git 기준 (`md`) | 제출 기준 (`docx`) | 현재 운영 기준 | 비고 |
|---|---|---|---|---|
| 프로젝트개요서 | `logmile_프로젝트개요서.md` | `docx/logmile_프로젝트개요서_v1.5.docx` | `v5.3` | 프로젝트 방향과 범위의 최상위 기준, 2026.05.26 3열 및 오토 플레이 시뮬레이션 반영 |
| 요구사항정의서 | `logmile_요구사항정의서.md` | `docx/logmile_요구사항정의서_v1.5.docx` | `v5.3` | 기능 범위와 인수 기준의 중심 문서, 3열 레이아웃 개편 및 오토 시나리오 일괄 시퀀서 반영 |
| DB 설계서 | `logmile_DB설계서.md` | `docx/logmile_테이블정의서_v1.5.docx`, `docx/logmile_ERD_v1.5.docx` | `v5.3` | `init.sql`/`seed.sql` v5.0 기준 구조 유지, 다중 이력 localStorage 보존 Rationale 반영 |
| 프로젝트수행계획서 | `logmile_프로젝트수행계획서.md` | `docx/logmile_프로젝트수행계획서_v1.5.docx` | `v1.5` | 일정, 마일스톤, 브랜치 운영 계획 기준, `docx`는 보관본 |
| 프로젝트구조도 | `logmile_프로젝트구조도.md` | `docx/logmile_프로젝트구조도.docx` | `v5.3` | 실제 패키지/API/FE 연동 상태 및 시뮬레이션 3열 개편 및 스토어 명세 보강 반영, `docx`는 보관본 |

## 3. 현재 남은 산출물 정리표

### 3.1 핵심 관리 문서

| 파일명 | 형식 | 분류 | 현재 상태 | 관리 기준 | 후속 작업 |
|---|---|---|---|---|---|
| `logmile_프로젝트개요서.md` | md | 핵심 기준 문서 | 유지 | Git 원본 | 수정 시 문서 버전 기준으로 변경 이력 보고 |
| `logmile_요구사항정의서.md` | md | 핵심 기준 문서 | 유지 | Git 원본 | 수정 시 문서 버전 기준으로 변경 이력 보고 |
| `logmile_DB설계서.md` | md | 핵심 기준 문서 | 유지 | Git 원본 | ERD/테이블정의서와 용어 정합성 유지 |
| `logmile_프로젝트수행계획서.md` | md | 핵심 기준 문서 | 유지 | Git 원본 | 수정 시 문서 버전 기준으로 변경 이력 보고 |
| `logmile_프로젝트구조도.md` | md | 핵심 기준 문서 | 유지 | Git 원본 | 수정 시 문서 버전 기준으로 변경 이력 보고 |

### 3.2 보조 관리 문서

| 파일명 | 형식 | 분류 | 현재 상태 | 관리 기준 | 후속 작업 |
|---|---|---|---|---|---|
| `logmile_WBS_초안.md` | md | 일정/업무 분해 | `초안` | Git 원본 | 초안 완료 전까지 `v1.x` 유지 |
| `logmile_코딩컨벤션가이드_초안.md` | md | 개발 표준 | `초안` | Git 원본 | 산출물/구현 기준 확정 후 보완 |
| `logmile_깃허브_브랜치전략.md` | md | 형상관리 문서 | 유지 | Git 원본 | 최신 브랜치 정책 및 AI 학습 단계 반영 |
| `logmile_팀원별_기능구현_브랜치순서.md` | md | 작업 순서 문서 | 유지 | Git 원본 | 요구사항 기준선과 맞춰 재점검 |
| `logmile_ERD.md` | md | DB 보조 문서 | 유지 | Git 원본 | `v1.7` 기준으로 시연용 입력 매핑 기준 반영 |
| `logmile_테이블정의서.md` | md | DB 보조 문서 | 유지 | Git 원본 | `v1.7` 기준으로 시연용 입력 매핑 기준 반영 |
| `logmile_최상위관리자_수정사항정리.md` | md | 변경 이력/메모 | 유지 | 참고 문서 | 최신 기준 반영 여부 점검 |
| `logmile_AI_모델재학습절차.md` | md | AI 재학습 절차 | 유지 | Git 원본 | `v1.0` 기준으로 버전 표기 정리 완료 |
| `logmile_관리자권한구조_변경사항.md` | md | 권한 구조 변경 | 유지 | Git 원본 | `v1.0` 기준으로 버전 표기 정리 완료 |
| `logmile_릴리즈전략.md` | md | 릴리즈 전략 | 유지 | Git 원본 | `v1.0` 기준으로 버전 표기 정리 완료 |
| `logmile_산출물_현황분석_및_수정가이드.md` | md | 산출물 현재화 가이드 | 신규 | Git 원본 | 실제 구현 기준으로 요구사항/DB/구조도/WBS/API 명세 수정 방향 관리 |

### 3.2.1 2026.05.14 실제 구현 검증 기준

| 영역 | 실제 기준 파일/폴더 | 산출물 반영 기준 |
|---|---|---|
| 권한/인증 | `logmile_be/src/main/java/com/project/logmile/common/enums`, `domain/auth`, `domain/admin` | `ROLE_SUPER_ADMIN`, `ROLE_ADMIN`, `PENDING/ACTIVE/INACTIVE/REJECTED/SUSPENDED` |
| DB 구조 | `logmile_infra/db/init.sql`, `logmile_infra/db/seed.sql` | `company`, `admin`, `vehicle`, `driver`, `drive_log`, `gps_data`, `rest_event`, `fatigue_event`, `plate_event`, `fatigue_threshold` |
| API 경로 | BE Controller 및 FE `src/api/*.js` | `/api/drive-history`와 `/api/drive-logs` alias, `/api/thresholds`와 `/api/fatigue/thresholds` alias 반영 |
| 번호판 이벤트 | `domain/plateevent`, `common/enums/Plate*` | `PlateEvent`, `ENTRY/EXIT`, `HIGHWAY_GATE/REST_AREA/CCTV`, `OCR/SIMULATOR/MANUAL/DUMMY` |
| FE 상태 | `logmile_fe/src/views`, `logmile_fe/src/api` | API 연동 화면과 mock 기반 화면을 구분 |

### 3.2.2 2026.05.15 시연용 시뮬레이션 기준

| 영역 | 기준 |
|---|---|
| 구조 원칙 | 실제 DB 테이블과 enum은 확장하지 않고 기존 구조를 유지한다. |
| 시연 입력 | 실제 자동 수집 대상인 번호판 인식, GPS 수신, CCTV 관측 데이터를 페이지에서 직접 입력해 재현한다. |
| A/B/C 프리셋 | 정상/주의/위험 대표 시나리오를 페이지에서 선택하면 이벤트와 피로도 흐름이 즉시 표시되어야 한다. |
| 저장 매핑 | 시연 입력은 기존 `drive_log`, `plate_event`, `rest_event`, `fatigue_event` 흐름에 맞춰 반영한다. |
| 위치 분류 | enum 확장 없이 `HIGHWAY_GATE`, `REST_AREA`, `CCTV`만 사용하고 세부 장소명은 화면 표시 또는 메모성 정보로 관리한다. |

### 3.3 제출용 문서

| 파일명 | 형식 | 분류 | 현재 상태 | 관리 기준 | 후속 작업 |
|---|---|---|---|---|---|
| `docx/logmile_프로젝트개요서_v1.5.docx` | docx | 제출/보관본 | 유지 | 보관 기준 | 요청 시 최신 `md` 버전 기준으로 갱신 |
| `docx/logmile_요구사항정의서_v1.5.docx` | docx | 제출/보관본 | 유지 | 보관 기준 | 요청 시 최신 `md` 버전 기준으로 갱신 |
| `docx/logmile_프로젝트수행계획서_v1.5.docx` | docx | 제출/보관본 | 유지 | 보관 기준 | 요청 시 최신 `md` 버전 기준으로 갱신 |
| `docx/logmile_ERD_v1.5.docx` | docx | 제출/보관본 | 유지 | 보관 기준 | 요청 시 최신 `md` 버전 기준으로 갱신 |
| `docx/logmile_테이블정의서_v1.5.docx` | docx | 제출/보관본 | 유지 | 보관 기준 | 요청 시 최신 `md` 버전 기준으로 갱신 |

### 3.4 판본 확인 필요 문서

아래 문서는 남아 있지만, 현재 기준 문서인지 보관본인지 구분이 더 필요하다.

| 파일명 | 형식 | 분류 | 현재 판단 | 권장 처리 |
|---|---|---|---|---|
| `docx/logmile_프로젝트개요서_v1.4.docx` | docx | 구버전 제출본 | `v1.4` | 참고 보관 여부 결정 |
| `docx/logmile_요구사항정의서_v1.4.docx` | docx | 구버전 제출본 | `v1.4` | 참고 보관 여부 결정 |
| `docx/logmile_ERD.docx` | docx | 보관본 | 기존 보관본 | 추후 docx 정리 시 판본명 정리 |
| `docx/logmile_테이블정의서.docx` | docx | 보관본 | 기존 보관본 | 추후 docx 정리 시 판본명 정리 |

### 3.5 외부 양식/참고 자료

| 파일명/폴더 | 형식 | 분류 | 현재 판단 | 권장 처리 |
|---|---|---|---|---|
| `WBS 양식/` | md, csv | 외부 양식 | 참고용 | 원본 양식으로 유지 |
| `straffic/` 하위 문서 | hwp, docx | 제출용 별도 폴더 | 제출본 관리 대상 | 핵심 기준 문서와 별도 목차로 운영 |
| 임시파일 및 시스템 파일 | - | 정리 대상 아님 | 현재 폴더 기준 미검출 | 신규 생성 시 즉시 정리 |

## 4. 현재 기준 판정

| 항목 | 현재 기준 |
|---|---|
| 문서 체계 | `md` 중심 관리 + `docx` 요청 시 제출/보관용 생성 |
| 버전 체계 | 관리 문서는 `v1.x`, 구현 반영 핵심 문서는 해당 문서 최신 버전 우선 |
| 핵심 기준선 | `프로젝트개요서`, `요구사항정의서`, `DB설계서`, `프로젝트구조도` |
| 초안 문서 처리 | 초안 완료 전까지 `v1.x` 안에서 보완 |
| 구버전 문서 처리 | `v1.4` 및 판본명이 불명확한 docx는 보관 후 추후 정리 |

## 5. 바로 이어서 할 작업

1. 요구사항정의서, DB 설계서, 프로젝트구조도는 2026.05.15 시연용 시뮬레이션 기준을 반영한 `v5.2`를 우선 기준으로 둔다.
2. WBS는 `v1.8` 기준으로 A/B/C 프리셋, 직접 입력 이벤트, 후속 API 보강 작업을 추적한다.
3. 산출물 수정 시 `md` 문서를 먼저 수정하고 변경 버전을 보고한다.
4. `docx`는 사용자가 요청한 경우에만 최신 `md` 기준으로 생성 또는 갱신한다.
5. 기존 `docx` 및 `v1.4` 문서는 제출/보관용 폴더에 유지하고 추후 정리한다.
