# ─────────────────────────────────────────────
# logmile 번호판 탐지 데이터셋 설정
# YOLO11n 학습용 data.yaml
# ─────────────────────────────────────────────

path: ../dataset        # dataset 루트 (data.yaml 기준 상위)
train: images/train     # 학습 이미지 경로
val: images/val         # 검증 이미지 경로

# 클래스 수
nc: 1

# 클래스명
names:
  - license_plate
